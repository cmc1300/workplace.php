
$(document).ready(function(){


	/* ---- Countdown timer ---- */

	$('#counter').countdown({
		timestamp : (new Date()).getTime() + 51*24*60*60*1000
	});


	/* ---- Animations ---- */
/*
	$('#social a').hover(
		function(){ $(this).animate({ top: 2 }, 'fast'); },
		function(){ $(this).animate({ top: 0 }, 'fast'); }
	);
*/

	/* ---- Using Modernizr to check if the "required" and "placeholder" attributes are supported ---- */
/*
	if (!Modernizr.input.placeholder) {
		$('.email').val('your email address...');
		$('.email').focus(function() {
			if($(this).val() == 'your email address...') {
				$(this).val('');
			}
		});
	}

	// detecting if the browser is Safari
	var browser = navigator.userAgent.toLowerCase();

	if(!Modernizr.input.required || (browser.indexOf("safari") != -1 && browser.indexOf("chrome") == -1)) {
		$('form').submit(function() {
			$('.popup').remove();
			if(!$('.email').val() || $('.email').val() == 'your email address...') {
				$('form').append('<p class="popup">Please enter your email.</p>');
				$('.email').focus();
				return false;
			}
		});
		$('.email').keydown(function() {
			$('.popup').remove();
		});
		$('.email').blur(function() {
			$('.popup').remove();
		});
	}
    */

});

